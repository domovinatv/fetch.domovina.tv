# Logs
*.log
*.log.gz
*.log.1
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime artefakti (lockfile, log dir, claude session lock)
automatic/logs/
.claude/

# System
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Dependencies
node_modules/

# Cookies & Secrets
automatic/cookies.txt
.env

# State & History
*-state.json
*-done.json

# Local storage symlinks (per-machine config)
storage/
storage.conf
.r2_md5_cache.json

# Loudness analysis artefakti (regenerabilni; trajni zapis je docs/loudness_analysis_2026-05.md)
loudness_data.json
loudness_report.json
loudness_report.xlsx
loudness_before_after.json
.firecrawl/

# Per-machine R2 keys caches (regenerabilni, --verify-r2)
.r2_keys_cache.json
.r2_video_cache.json
automatic/full_backfill_state.json

# Podscan.fm sirovi per-request cache i dnevni budžet (regenerabilno, veliko).
# data/podscan_hr_corpus.json se NE ignorira — trial kvota je 100 req/dan,
# pa je zrcalo skupo za ponovnu izgradnju i vrijedi ga imati u repou.
data/podscan_cache/
data/podscan_firehose.jsonl
# Izvještaj se regenerira iz zrcala u sekundi, bez API poziva — ne u repo.
data/podscan_coverage_report.json
